{
  "name": "hwpit-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Anthropic API passthrough proxy for HWP it! (Cloudflare Worker)",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
