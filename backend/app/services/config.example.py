# 이 파일을 config.py 로 복사한 뒤 실제 값을 채우세요. (config.py 는 git에 올라가지 않습니다.)
#
# PROXY_BASE_URL : 배포한 Cloudflare Worker 주소 (뒤에 /v1/messages 는 붙이지 마세요)
# APP_TOKEN      : Worker에 등록한 APP_TOKEN 과 동일한 값

PROXY_BASE_URL = "https://hwpit-proxy.<your-subdomain>.workers.dev"
APP_TOKEN = "worker에-설정한-것과-같은-토큰"
